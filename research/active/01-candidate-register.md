# Candidate Register

Candidates are hypotheses, not commitments. A candidate must be marked HOLD until it passes the direct-neighbor and feasibility gates.

| ID | One-sentence problem | Story and harm | Exact CS contribution / claim | Bob Zhang adjacency and non-overlap | Status | Main kill condition | Next evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C1 | Low-cost CO2 node decides `alert / no-alert / inspect` under calibration ageing. | Facilities operator can miss a ventilation response or waste staff time; the proposed public evidence was outdoor reference co-location, not this workflow. | Calibration-age-aware residual risk policy at a fixed inspection budget. | General imperfect-observation adjacency only; no PAMI method is reused. | **KILL** | The public data lack the indoor action setting, required raw features, threshold-supporting range, and an identifiable age-to-error relationship. | A new candidate would need an authorized indoor, traceable-reference deployment and an action-cost protocol; it is not a continuation of C1. |
| C2 | Small-farm operator defers irrigation when cheap soil-moisture and water-balance model disagree. | Bad irrigation wastes water or stresses crops. | Bounded, disagreement-aware `irrigate / wait / inspect` decision under fixed water and plant-risk costs. | General reliability adjacency only; no PAMI method is reused. | **KILL** | Recent direct work already covers low-cost capacitive sensors, water-balance/physical models, irrigation harms, thresholds and human oversight; a one-probe/model residual cannot identify sensor fault versus model/input/soil heterogeneity. | Retain only the protocol lesson: require independent truth, explicit inspect cost and hysteresis-plus-inspection baseline for any future disagreement claim. |
| C3 | Edge maintainer decides whether to continue, observe, or fail over an on-device periodic sensing/inference service under resource contention. | In a non-production lab monitor, a false failover creates an unnecessary gap/availability mark in the local sensing record; a late action extends a real, labelled service-contract breach. Local observations and action budgets matter while the WAN/remote probe is unavailable or too late. | Source-only telemetry-policy freezing and target-cell evaluation. | General reliability adjacency only; no PAMI method is reused. | **KILL** | UCC 2025 already asks the cross-device/workload/stress timeout-transfer question on Pi/Jetson with the same stressors and false-positive/delay/overhead endpoints. EdgeStressBench 2026 occupies the apparatus framing. The residual freeze is experimental hygiene, not a distinct research mechanism; the action's restoration benefit and selection inference are also unspecified. | Retain only a bounded stock-board replication/negative appendix comparing `phi + observe` with a second-probe or lower-layer alternative. |
| C4 | Node chooses raw transmission, local processing, or suppression before a time-sensitive threshold action. | A premature/inaccurate or stale estimate can cause a wrong maintenance action. | Decision-margin scheduler under fixed compute/transmission budget. | General reliability adjacency only; no PAMI method is reused. | **KILL** | Ballotta et al. already formulate raw-versus-local-processed sensing under compute/communication latency and constrained resources. In the current story, `suppress` is only a threshold action and the margin is a reformulation. | Retain only a scalar-action-loss equivalence counterexample, not a mechanism thesis. |
| E | Low-power sentinel selectively wakes a costly verifier, leaving no-wake episodes incompletely observed. | A simulated local safety-assist zone may miss an object while trying to avoid continuous sensing; it must not be represented as a certified interlock. | Former risk--energy--latency audit pivot. | Reliability under imperfect observation only; no PAMI method is reused. | **KILL** | Marinov et al. already combine continuous low-power sentinel, selective high-power activation, variation triggering, controls and energy--fidelity evaluation. An original-time reference improves offline scoring only. | Retain only sentinel-predictiveness versus fixed periodic sampling as a negative control. |
| D1 | A bench node emits `clear / uncertain` for a simulated protected envelope using a low-cost multi-zone ToF sensor. | A false clear lets an object occupy the envelope; unnecessary uncertainty lowers availability. | Former finite physical condition-map pivot. | General imperfect-observation adjacency only; no PAMI method is reused. | **KILL** | Gimpelj & Munih already measure dToF geometry, gaps, speed, background and reflective materials for conservative configuration. A multi-zone board or finite map changes neither task nor endpoint. | ToF preflight control only. |
| D2 | A proximity node may run a reference-target self-test before indicating a simulated safe-to-close state. | A false safe could leave an object in the envelope; a needless test costs interruption/energy. | Reference-target self-test and risk-triggered active diagnosis. | General reliability adjacency only; no PAMI method is reused. | **KILL** | Reference-target self-tests and active diagnosis are established; a passing reference ray cannot certify the whole protected envelope. | Retain only as a D1 fault-coverage measurement appendix, with no safety or mechanism claim. |
| T1 | Receiver decides whether a rebooted/delayed timestamped sensing record is fresh. | An old record accepted as fresh corrupts a maintenance record; unnecessary defer creates a gap. | Epoch/sequence/clock-quality three-action freshness policy. | General trustworthy-observation adjacency only. | **KILL** | Standard epoch + sequence + calibrated clock-error interval implements the same deterministic deadline guard; a receiver-only capture cannot label acquisition time. | Retain epoch/sequence/clock-error bookkeeping only as C3 trace-quality instrumentation. |
| B1 | Asset operator records or abstains on low-cost BLE proximity. | False record corrupts inventory; abstention requires manual work. | Finite physical-panel RSS abstention boundary. | General imperfect-observation adjacency only. | **KILL** | Cortesi et al. 2025 already cover low-power BLE/RSSI asset attribution, edge-local preprocessing, `SURE / UNSURE` triage, independent physical truth and energy. Their near/far RSS overlap also defeats broad RSS-only inference. | Retain only a non-personal RSS-ambiguity measurement as an appendix for another candidate. |
| AS1 | Lab technician decides whether a cheap sentinel warrants an expensive verification measurement. | A skipped, short threshold excursion can create a false `no-event` record; continuous verification costs energy/storage. | Former equal-budget audit pivot. | General imperfect-observation adjacency only. | **KILL** | Selective wake/coverage/cost is established; fixed maximum-gap periodic sampling is the required counterbaseline when timing is uninformative. | Evaluation discipline only. |
| F5 | A benchtop record maintainer decides `accept / issue harmless physical challenge / unknown` when an acoustic or vibration trace may be replayed. | Accepting replay corrupts an experimental record; probing all intervals uses energy/time. | Former selective local physical-challenge pivot. | General trustworthy sensing/security adjacency. | **KILL** | Chen et al. already inject excitation and compare actual with expected sensor response under uncertain delays; Zhao et al. already use an event-triggered physical-watermark schedule for replay attacks under performance cost. A challenge observed in advance is replayable; a fresh secret challenge is a watermark. | Retain only as a security demonstration/replication, not a research mechanism. |
| Q1 | Lab asset-counting operator records a tabletop passage as `count` or `unknown` using a low-cost local camera. | A wrong retained count corrupts a maintenance record; `unknown` uses a finite review budget. Raw video is not retained or sent to a remote service. | Frozen detector-margin plus observable image-quality rule, evaluated on a held-out camera/degradation block against margin-only, quality-only, and periodic-review rules. | General selective-classification adjacency only; no PAMI method is reused. | **KILL** | Gao et al. 2025 already combine detector/count confidence, image quality/clarity and physical condition to predict counting confidence; score-to-abstain is a direct simple baseline. Local-only retention does not itself create an edge-dependent endpoint. | Retain only source-frozen camera/condition-blocked evaluation, independent episode truth and review-budget reporting as protocol discipline for another candidate. |
| S1/S2 | A maintenance-record operator must distinguish scheduled samples that are complete, late-but-recoverable, or unknown after an outage, and decide whether to wait for synchronization or inspect the source node. | A false `complete` hides a missing record; false attribution wastes maintenance effort. Edge matters only because the source retains persistence/boot facts during disconnection. | **Boundary candidate only:** paired executions establish when receiver-only traces cannot distinguish `sampled-and-persisted then partitioned` from `stopped before sampling`; a finite source-evidence schema is compared with receiver-only evidence and a durable-journal verifier under stated faults. | General edge reliability only; no PAMI method is reused. | **KILL** | Receiver-side indistinguishability is a standard failure-detector boundary; source state cannot alter an outage-time action before reconnection; a crash-atomic B-journal subsumes the schema retrospectively. | Retain only a device-specific crash-window B-journal replication/negative appendix, with no live attribution or source-observability mechanism claim. |
| V1 | A lab camera keeps bounded local evidence to support later diagnosis of a missed object/fault episode. | Retaining the wrong clip/log prevents diagnosis; retaining all video violates the fixed local storage/privacy budget. | Source-side selection of clips, reason codes and hashes to improve blinded post-hoc root-cause recovery at fixed bytes. | General trustworthy-vision adjacency only; no PAMI method is reused. | **KILL** | Hindsight already retroactively retains local trace evidence after a detected symptom; Reverb 2026 reconstructs faulty IoT executions from edge-side recordings to identify root causes. | Retain only an evidence-retention benchmark/implementation control, not a thesis mechanism. |
| AVS | An environmental node chooses `virtual / wake-and-read / unknown` while a high-power sensor is inactive. | A wrong virtual value corrupts a maintenance record; waking excessively exhausts the declared read/energy budget. | Frozen support-gated virtual sensing with tri-state output. | General low-power sensing adjacency only; no PAMI method is reused. | **KILL** | Marinov et al. 2026 already combine a continuous low-power sentinel, selectively activated high-power sensor, variation trigger/scheduling, and energy--fidelity evaluation; WONS 2026 also covers intentionally inactive virtual sensors from companion/history observations. `support` and `unknown` are rule variants, not a distinct mechanism. | Retain only as a reproduction/control for a future physical study. |
| VSB | A benchtop maintainer sees an ambiguous vibration trace and decides whether to record an enumerated mechanical condition, mark measurement uncertainty, or inspect the setup. | Mistaking a loose mount for a mechanical trend wastes inspection; missing the specified imbalance condition corrupts a maintenance record. | Former pivot: a reference-instrumented finite mount/load ambiguity map. | General sensor-integrity/measurement adjacency only; no PAMI method is reused. | **KILL** | Karumanchi's official 2025 thesis already studies a MEMS device's self-detection of fixture looseness across four materials, validates it with a laser vibrometer, documents fixture/orientation/real-machine limits, and explicitly proposes real-machine profiles plus resource-constrained MCU deployment. A finite map would be an unnew continuation or instrumentation study. | Retain the mount-transfer/mechanical-spectrum ambiguity as a preflight control for any future vibration study; do not make it the claimed contribution. |
| TVA | A benchtop maintainer uses a low-power sentinel to decide whether to wake a verifier after a short test event. | Using a verifier reading that begins after the event as ground truth corrupts a missed-event measurement. | Interval logs, original-time reference, and `not label-admissible` outcome for post-ready observations. | General sensing-evaluation adjacency only; no PAMI method is reused. | **KILL** | Finite wake-up and short-event overlap is an established duty-cycling condition; fixed `coverage => label, otherwise UNKNOWN` implements the entire honest output. Continuous reference is an offline evaluation control, not a resource-neutral edge mechanism. | Retain readiness interval logging and a duration/latency map only as a negative-control appendix. |
| SAP | A lab technician decides whether a non-personal tabletop record is supported, needs a declared diagnostic/re-acquisition action, or must go to review. | A false accepted record creates bench rework; an unnecessary intervention consumes a fixed operator/capture budget. No production or safety action is authorized. | **Boundary only:** an independently measured diagnostic observable or a predeclared one-step intervention changes post-action record risk beyond an equally informed cost-sensitive router on blocked physical-fault versus healthy-hard-scene cells. | Incomplete observation / uncertainty adjacency only; no PAMI method is reused. | **PIVOT (Amber)** | If the structured policy only transforms the same passive observations, it cannot exceed a Bayes-consistent router supplied those observations. RASMF 2026 is a direct four-action, cost-sensitive quality-router baseline. | Specify one independent witness or discriminating intervention, concrete technician/object/record criterion, action outcome, sensor API and budget. Otherwise retain only a well-labelled negative benchmark. |
| EBR-AIM | A lab technician maintains a non-production edge sensor node after an I2C read failure. | A poor recovery choice prolongs a local data gap, loses samples, or writes a collateral register value; an unnecessary power-cycle costs time and energy. | Former conditional boundary claim: independently counted probe versus staged recovery. | Embedded interface reliability adjacency; Linux/TI/NXP and US20250068503 already cover recovery mechanisms and action family, while SentryBus covers generic observability. | **PIVOT / historical** | Retained only as a documented negative/replication path. | No longer current direction; preserve for audit traceability. |
| P1 | A verifier provider wants cross-device palm robustness without making the same user easier to link across systems. | Cross-device false rejects deny access; device-invariant templates may increase cross-service tracking. | Conditional protocol claim: under a fixed extractor and edge budget, measure the cross-device EER versus maximal-linkability frontier; no monotonicity is assumed. | Shen 2022, Jia 2025 and AdaptPFL 2025 cover cross-device generalization; Ali 2024 and maximal-linkability 2024 cover privacy metrics separately. | **HOLD (Amber)** | If standard alignment plus salted templates reproduces the frontier, retain only an audit. | Confirm paired identities/devices, ISO threat model and linkability implementation. |
| P2-CTB | A local palm matcher stores a template; a leak may let an attacker present a reconstructed palm to another edge device. | Unauthorized acceptance and irreversible biometric privacy loss. | Former direct-ROI template-to-image-to-matcher boundary claim. | Yan 2024 covers template reconstruction with cross-algorithm/cross-dataset matching; Yang 2023 covers cross-database template inference; EMPalm and CAAP cover distinct multi-device/physical-transfer observations. | **KILL / superseded** | Direct ROI injection omits B capture/ROI/PAD and a null is non-identifying; a larger device x sensor x model matrix is an evaluation sweep, not a new endpoint. | Retain only an explicitly labelled reproduction or Pi runtime demonstration after data/ethics approval. |
| P3 | A device fleet wants local protected matching while preserving accuracy and preventing cross-application linking. | A scheme can retain EER on one device but fail after revocation, multi-template linking or inversion on another. | Conditional audit of EER/FMR/FNMR, revocation, unlinkability, inversion and cross-device degradation; no new protection transform. | Ali 2024, PalmSecMatch 2024, Deep Secure PalmNet 2024 and ISO/IEC 24745 already occupy the metric package. | **KILL broad / audit only** | If a published scheme already passes the registered matrix, this is replication. | Use only as P2 protection baselines if time permits. |
| P4 | A malicious client queries an edge matcher and adapts probes from feedback. | Query abuse can produce an impersonation; rate limits and hard/soft feedback affect edge cost. | Conditional attack benchmark only; no new adaptive attack without a new feedback channel or physical constraint. | Yan 2024, DifAttack 2024, FeatureBA 2025 and CAAP 2026 occupy black-box/transfer attack families. | **KILL** | Standard Boundary/DifAttack or rate-limited random search matches success/query. | Optional sandbox control for P2, not the thesis. |
| P5 | A local verifier must accept, reacquire or reject under unseen device, blur, low light and open-set users. | False accept is an access error; unnecessary review costs time. | Conditional calibration/abstention audit under fixed review/energy budget. | UCFace 2024, Gallery-Aware Uncertainty 2024, PalmRSS 2025 and PalmBridge 2026 occupy generic uncertainty/open-set robustness. | **KILL generic** | Score-only or quality-only routing matches risk-coverage on held-out conditions. | Keep as a negative control for P2 target presentation quality. |
| P6 | NIR or another palm modality is unavailable on a low-cost node. | Forcing fusion may hallucinate identity cues; waking/reacquiring costs energy and time. | Conditional edge-budgeted fuse/single/reacquire/abstain protocol under declared missingness. | SSFD-Net 2025, DPFed-Palm 2025 and X-Palm 2026 occupy missing/cross-spectral recognition. | **KILL generic / PIVOT audit** | SSFD-Net-style reconstruction matches policy at equal cost, or missingness is not independently labeled. | Optional baseline only; no separate method claim. |
| BIA-1 | A lab must decide whether a cross-domain palm result supports an intervention on a named device/sensor factor or only a broad domain-shift statement. | Misattributing pose/light/ROI effects to hardware, template protection or an edge matcher leads to unsupported deployment/security claims. | Former attribution-sufficiency certificate proposal. | ISO/IEC 19795-10:2024 already requires controlled factor plans, uncertainty and disclosure when attribution is not unique; DISDE/Roschewitz occupy generic performance-shift diagnosis. | **KILL standalone / preflight only** | X-Palm lacks repeated cross-classified device/nuisance support, and the surviving output is a standard broad-domain report or checklist. | Keep as mandatory preflight for any future biometric benchmark; do not allocate the FYP research slot. |
| PHT-2R | A benign water-test training bench decides whether to accept a pH interval record, perform a buffer check, or mark it unknown. | A wrongly accepted bench record causes rework; excessive checks consume finite buffer/time. | Former claim that policy-selected separated buffer pairs resolve gain versus offset ambiguity beyond fixed calibration schedules. | Cho et al. 2024 already automate one- and two-point pH/EC drift handling; Hurst 2025 schedules calibration by uncertainty. | **KILL** | In the declared affine equal-noise model, a maximum-separation two-point pair is fixed D-optimal; the remaining accept/unknown output is standard interval reporting. | Retain only a routine fixed one/two-point calibration control. |
| LENS-RC | A non-personal tabletop imaging bench decides whether to retain a record, capture a fixed chart, or mark camera health unknown. | A false health-clear can cause an untrustworthy local bench record; chart captures consume a declared time/energy budget. | Former chart-conditioned camera-health routing claim. | ISO 12233:2024/IEEE 1858-2023 cover chart image-quality measurement; Ma 2026 covers passive monitoring; Heuillet et al. 2024 covers costly partial monitoring. | **KILL; optional preflight only** | A chart observes the imaging path, not scene truth; an equally informed cost-sensitive router, passive monitor or fixed-period chart eliminates the claimed policy mechanism. | Optional physical gate only: a named camera-path condition with passive, fixed-period and always-chart controls; no thesis promotion from either result. |
| RWE | A bench technician decides whether an edge node may resume non-sensitive test recording after an interrupted signed update. | A false release could corrupt a local experimental record; needless recovery wastes a working run. | Former external read-only boot/update witness for `release / recover / unknown`. | RFC 9019 and ESP-IDF OTA already expose verified manifest/state, pending verification, self-test, rollback and anti-rollback. | **KILL** | Under the benign fault model, reading the same immutable bytes adds no information; later mutable faults remain outside both observations. | Use stock OTA rollback only as a systems control. |
| SDC-X | A lab decides whether to retain, recompute or mark unknown a deterministic non-safety edge result. | A silent wrong retained score wastes later bench work; recomputing costs time and energy. | Former heterogeneous companion-board recomputation under seeded post-input/pre-output corruption. | Xue and Zwolinski 2024, Magliano et al. 2025, and N-version redundancy cover duplicate/golden comparison and real-hardware injection. | **PIVOT / negative benchmark only** | The injected one-replica fault model makes diversity success tautological; two agreeing outputs still leave common input/specification/post-comparison faults. | Retain only as a labelled fault-injection teaching/control benchmark; no general correctness claim. |
| FSD-WIT | A lab logger decides after a controlled power cut whether its last synthetic record may be retained or is unknown. | Retaining a torn record corrupts bench analysis; discarding every recent record wastes a run. | Former configuration-scoped post-reboot false-retain boundary claim. | CrashMonkey/ACE, Pathfinder 2025 and Ahmadian et al. already combine bounded crash states or physical power cuts with external recovery oracles. | **KILL; negative appendix only** | A standard two-phase valid-prefix scan already implements `retain / unknown` for synthetic records; it cannot establish real acquisition truth, while cross-card positive transport is statistically/physically unidentifiable. | At most publish a labelled configuration-recovery dataset with no mechanism, durability-certificate or cross-card claim. |
| POF-ADM | A bench logger sees a power transient while recording and decides whether to keep an interval or mark it unknown. | A false keep may preserve a record disrupted by undervoltage; indiscriminate discard wastes a run. | Former power-fail-aware record-admissibility claim. | Nordic POFWARN and ESP-IDF brownout facilities are standard reset/preparation mechanisms. | **PIVOT; instrumentation only** | Comparator/reset observation cannot witness opaque-media commit or sensor semantic truth; valid-prefix recovery can make it redundant. | Use only as a preregistered incremental-information measurement within an appendix. |
| TTA-AR | A low-cost edge node considers accepting, keeping or abstaining on a shadow test-time-adaptation update from an unlabeled stream. | Accepting a worse update corrupts local task output; needless abstention reduces availability. | Former observable proxy-validity region for source/candidate risk selection. | AETTA 2024, Kim et al. 2024 agreement-based selection, Schirmer et al. 2025 proxy-risk monitoring, and Drift2Act 2026 with delayed labels. | **KILL** | Identical inputs/logits/proxies/telemetry admit target label mechanisms with opposite risk order; no-label shadow execution adds no semantic observation. | Retain only the two-world screening counterexample; require independent task witness or a pre-proved restricted shift family before any future adaptation candidate. |
| AL-FactorQueue | An edge bench retains a few stream items for scarce manual labels to improve later local classification. | Missing a harmful factor cell leaves persistent local model error; reviewing too many items interrupts a limited bench session. | Former physical-factor-aware finite queue claim. | TActiLE 2025 occupies online irreversible stream selection, finite oracle labels, retraining and resource endpoints. | **KILL** | Known factor IDs reduce to factor-quota/stratified sampling; unknown factors are unavailable at selection; factor coverage alone does not identify unseen-cell labels. | Retain fixed-factor quota as a baseline/protocol control only. |
| UPM-EdgeBound | A frozen edge classifier has no labels after deployment and must continue, request review or suspend records. | False continue retains wrong records; needless suspension loses usable work. | Former trace-only impossibility plus minimal-witness claim. | Garg et al. 2022 unidentifiable target accuracy; Chen et al. 2022 restricted joint-shift family. | **KILL** | Input/prediction/telemetry traces can be identical under opposite target labels/risk; the claimed theorem is a direct instance, while a sufficient physical witness remains unspecified. | Retain as an identification screen, not a standalone boundary thesis. |
| QNN-DecisionGuard | A low-cost integer runtime decides whether a quantised binary result may be released or marked unknown. | A near-threshold numerical flip contaminates a local tally; excessive abstention wastes runs. | Former per-input quantisation sign-preservation guard. | QEBVerif and certifiable quantisation-robustness work. | **KILL; verification appendix only** | Existing quantisation error bounds/verification implement the claimed mathematical core. | Use existing verifier baselines only. |
| ASV-PathCheck | An acoustic bench chooses usable, tone-check or unknown for a microphone path. | A degraded path corrupts a non-production acoustic record; tones cost time/energy. | Former passive-triggered active acoustic path self-check. | Active acoustic sensor self-diagnosis and low-cost speaker/reference calibration. | **KILL; calibration bench only** | A tone observes an entangled microphone/speaker/room/placement path and fixed-period/always-tone controls absorb the policy. | Optional apparatus calibration only. |
| TRC-X | Before copying a frozen policy to a replacement low-cost sensing unit, a technician chooses release, trial-only or do-not-release after labelled acceptance trials. | False release creates unreliable bench records; needless quarantine delays a run. | Former crossed unit x load x fault acceptance rule. | Reliability demonstration testing, acceptance sampling and unit-heterogeneity models. | **KILL; protocol appendix only** | Three-state release is accept/reject/no-decision. Four to six devices cannot support a useful population claim; finite-matrix exhaustive testing is a standard table. | Preserve an evidence-sufficiency report only: observed-matrix performance is not new-device transfer. |
| MFD-Release | A frozen fault-alert policy is copied to a replacement low-voltage rotating demonstrator. | Wrong/missed records create bench rework. | Former passive pre-deployment risk-coverage gate. | 2025 cross-machine/cross-condition/open-set fault diagnosis. | **KILL** | Same task and held-out machine/condition endpoint; a board or motor replacement is not a new mechanism. | Do not reopen without a distinct observation/action/guarantee. |
| MQ-Guard | A low-cost quantitative node writes within-range, out-of-range or unknown after sensor replacement. | A wrong threshold decision invalidates a run; constant recalibration wastes time. | Former transfer guard-band policy. | Low-cost sensor validation, calibration and recalibration literature; PHT-2R constraint. | **KILL** | Guard bands/intervals and reference acceptance are standard calibration decisions. | Characterization only. |
| UOD-ReleaseBound | A frozen model sees only local input, output and telemetry after device/load/fault change. | False continue preserves wrong records. | Former no-witness transfer-refusal boundary. | Unlabeled target-accuracy impossibility and label-free monitor literature. | **KILL; negative screen only** | Same-observation/opposite-label construction is an existing general boundary. | Retain as a preflight screen for future claims. |
| FIP-Transfer | A maintainer treats finite seeded-fault success as a transfer guarantee. | A false inference reuses a policy outside tested mechanisms. | Former seeded-fault coverage-to-transfer claim. | Established fault-injection / SDC-X boundary. | **KILL; negative appendix only** | Seeded faults label only their own distribution; no identified mapping to future field faults. | Use as a counterexample/control, never a guarantee. |
| UWB-RL-CAUSE | A rail logger uses target and fixed-reference UWB links to retain or diagnose a bad range episode. | A wrong cause label corrupts a local calibration record; indiscriminate unknown wastes diagnosis time. | Former target-path versus infrastructure-wide ambiguity attribution claim. | Peterseil et al. 2024 multi-link UWB trustworthiness; Yang et al. 2025 CIR/range credibility and error-source mitigation. | **KILL; replication appendix only** | Passive radio summaries cannot generally identify physical cause; a Peterseil-style sequential multi-link monitor and equally informed classifier subsume the proposed policy. | At most test named DWM3000/DW3720 fixture/CIR extraction against published baselines, without causal or transfer claims. |
| CSI-BaselineGate | A Wi-Fi-CSI bench decides whether a presence episode is retainable or unknown. | False retain corrupts the local dataset. | Former auxiliary-link stale-baseline detection. | OpenCSI 2026 reliability/maturity/stale-baseline abstention. | **KILL** | Same local CSI reliability and abstain action family. | Protocol control only. |
| MAG-GeometryWitness | A magnetic rail fixture marks position readings geometry-consistent or unknown. | False consistency corrupts the fixture record. | Former field-manifold residual for rotation versus displacement. | Low-cost vector calibration and magnetic localization. | **KILL** | Calibrated localization/residual threshold and field non-uniqueness absorb the claim. | Apparatus preflight only. |
| MMW-ClutterWitness | An mmWave bench decides whether a fixture-change observation is usable or unknown. | Wrong retained state wastes a calibration run. | Former fixed-reflector path-drift check. | mmWave self-calibration and appliance-state sensing. | **KILL** | Reference reflection is not scene truth and standard self-calibration/fixed probes absorb the policy. | No thesis slot. |
| AQ-ReferenceFreeAudit | A cheap gas-sensor bench decides log, inspect or unknown. | False log mislabels a bench experiment. | Former reference-free interference-versus-ageing diagnosis. | On-device reference-free multi-pollutant self-diagnosis. | **KILL** | Direct Pi-class quality-assurance/fault-injection collision and no traceable truth fixture. | Do not pursue. |
| DCC-SPLIT | A record manager decides whether sensor records before and after a board/configuration transition may be joined. | A false join invents a trend; needless split reduces usable records. | Signed transition provenance plus version-aware segmentation. | DCC systems already carry signed calibration/procedure/reference metadata. | **KILL** | Metadata authenticates history but cannot prove numerical comparability without an independent overlap/reference model. | Use provenance/version segmentation as an existing control. |
| REUSE-WIPE | A lab decides whether to release, retire or retain a reused logger containing non-personal test material. | A false release may expose test material or credentials; needless disposal wastes a node. | Host-side/cryptographic media retirement boundary. | Autonomous IoT decommissioning and NIST sanitization scope directly cover the ordinary mechanisms. | **KILL** | Host-visible delete/readback cannot establish controller-level erasure; a scope matrix is not a new method. | Retain only as data-handling guidance. |
| OFT | A bench transfers an offline edge node between local projects. | Wrong admission can leave old credentials active; needless lock delays bench work. | Signed offline ownership/control transfer. | AutoPKI and lifecycle guidance occupy signed trust-transfer/attestation. | **KILL** | Without trusted time/counter, replay freshness is undecidable and the honest output is `unknown`; with one, standard protocols apply. | Retain only as a protocol constraint. |
| ACR-MODE | A non-personal tabletop logger chooses RGB, NoIR+IR or recapture under a fixed capture budget. | A wrong marker record creates bench rework; extra captures cost time/energy. | Generic adaptive camera/modality selection by preview quality or model confidence. | Lens 2025, AdaptiveISP 2024, AdaptiveAE 2025, CM-ASAP 2024. | **KILL** | Fixed mode, scalar brightness/blur threshold or published adaptive sensor-control structure matches. | D0 engineering control only. |
| BURST-COMP | A logger chooses a second exposure or frame after a preview. | Missed transient corrupts a non-production timing/count record; always-bracketing wastes budget. | Adaptive exposure/frame-rate scheduling. | AdaptiveAE 2025 and CM-ASAP 2024. | **KILL** | Fixed shortest exposure/periodic sampling matches at equal capture cost, or the timing oracle is not independent. | Control only. |
| EV-EXPO | A logger chooses a second short exposure or IR capture for a rotating marker/LED event. | Missed transient corrupts a timing/count record; always bracketing costs energy. | Adaptive exposure and selective camera triggering. | AdaptiveAE 2025; NeuroViG WACV 2025. | **KILL** | Fixed shortest exposure/periodic sampling matches; no independent timestamp oracle. | Control only. |
| MOD-MISSING | A node chooses RGB or NoIR when one optical path is unavailable. | False retained marker corrupts a local record; reacquisition costs energy. | Learned missing-modality and adaptive modality activation. | CM-ASAP 2024; Liao et al. CVPRW 2025; Rampure et al. 2026. | **KILL** | Brightness threshold/fixed mode or published modality-selection baseline matches. | Physical cells retained only for PHY-MASK. |
| CAL-TRANSPORT | A threshold calibrated on RGB is reused on NoIR. | A false decision creates a bad local record; recalibration costs a bench session. | Cross-device/cross-spectral calibration and domain shift. | PalmRSS 2025 and RGB/NIR calibration/registration work. | **PIVOT** | A fixed affine/quality correction fully explains the shift; no new action or guarantee remains. | Preflight/negative characterization. |
| PHY-MASK | Synthetic missing/noisy masks are compared with physical camera-path failures on a printed marker task. | Treating simulated robustness as physical reliability can cause false retained records; physical retakes cost time/energy. | Simulated EMM/RMM/NM versus real cable, illumination, saturation and lens faults. | Liao et al. CVPRW 2025 and missing-modality method families. | **PIVOT** | Synthetic and physical action rankings agree across blocked cells, or physical labels/oracle are not independent. | Negative benchmark only. |
| ASI-IR | A Pi preview chooses RGB-only or NoIR+IR for a printed code. | A wrong retained code corrupts a bench log; a second capture costs time/energy. | Adaptive camera control and modality selection. | Lens 2025, AdaptiveISP 2024, AdaptiveAE 2025, QIC SECON 2024 and Bian et al. Sensors 2026 occupy adaptive acquisition/resource/device-shift families; exact IR-on/off code endpoint remains unverified. | **KILL generic / PIVOT finite audit** | Fixed two-shot policy, scalar brightness/blur rule or conservative escalation matches the frontier. | Static physical pilot only; no generic adaptive-sensing claim. |
| OPT-WIT / ILLUM-CAUSE | IR on/off is used to distinguish a named transparent-cover/contamination state from low visible light. | False record contaminates a calibration/maintenance trace; cleaning/reacquisition costs time. | Passive lens contamination and camera-health monitoring. | Pao et al. 2024; Kim et al. 2025; Lens 2025. | **HOLD (Amber)** | Simple IR ratio or passive quality router matches; labels are not independent; or two-world observations are indistinguishable. | Requires fixture, lux and power measurements; no general camera-health claim. |
| FULL-CAP | A frozen marker decoder is evaluated under paired physical and digital capture corruptions. | Digital-only robustness can mislead a local dataset curator; manual review consumes a fixed budget. | R-Bench 2024 and simulated multimodal-failure benchmarks. | **PIVOT** | Digital corruption rankings match physical rankings and no action changes; report a null, not a method. | Backup negative benchmark. |
| PRF-TR | An edge-pipeline engineer decides whether digital robustness tests are sufficient to release a camera capture policy for a field-maintenance device. | A wrong synthetic ranking can ship a policy that retains a bad local record or causes unnecessary reacquisition in the field. | Finite paired digital/physical fault-cell ranking audit with exact marker truth and matched capture cost. | Agnihotri et al. CVPRW 2025 directly studies synthetic-vs-real proxy validity; MSC-Bench/Liao occupy simulated failure benchmarks. | **PIVOT (Amber; bounded evaluation/negative result)** | High rank correlation or a brightness/blur baseline ties; no independent labels/oracle; no preregistered inversion. | Do not claim a new method, deployment certification, or general transfer. Lock only as a finite evaluation study after the pilot. |
| IR-CAUSE | A technician decides whether IR-on adds information to distinguish low visible light from a named transparent-cover condition. | Wrong cause label pollutes maintenance traces; unnecessary reacquisition costs time/energy. | Narrow active IR value-of-information test with exact-code endpoint and two-world identifiability check; not a causal-diagnosis framework. | SIDL 2025 and Ma 2026 occupy physical contamination/passive health monitoring; Han 2025, Yu 2025 and Bian et al. Sensors 2026 occupy active diagnosis/acquisition and device-shift escalation. | **PIVOT (Amber)** | Fixed two-shot/IR-ratio/passive quality/conservative escalation baseline ties, or matched worlds are indistinguishable. | Requires independent cover labels, manual/logged AE/WB and power; lux is optional logging only. |
| FLICK-PHASE | A logger chooses exposure phase or recapture under measurable LED flicker. | Rolling-shutter banding causes false marker decisions; waiting costs latency/energy. | Phase-aware action measured against a photodiode oracle. | Ren et al. 2024 adaptive active exposure for fiducial markers; VCIP 2025 flicker removal; Otsuka APCC 2024 synchronization; Fukuda 2024 timing apparatus. | **KILL thesis; timing oracle only** | Fixed anti-flicker settings or always-IR match; the exposure/calibration mechanism is directly occupied. | A photodiode/LED board may only validate timing in XFER-ACT/PRF-TR. |
| PWR-WIT | A node decides whether a frame taken during IR/CPU load may be retained. | Supply transients can silently corrupt a local record; blanket discard wastes work. | Power trace as conditional witness for false-retain at matched image-only cost. | Brownout/reset facilities and sensor-fault testing are established controls. | **HOLD (Amber; likely instrumentation)** | Image-only quality/frame-valid metadata match, or trace is not temporally aligned. | Safe programmable droop and power measurement required. |
| RS-DRIFT | A maintainer gates sequential RGB/NoIR fusion when readout timing may drift. | False registration yields a wrong marker/pose; timing check costs a short probe. | Bounded downstream reliability gate, not a new readout estimator. | RSSC 2026 self-calibration and Fukuda 2024 timing apparatus are direct neighbors. | **PIVOT/KILL** | Factory constant or one-time calibration predicts the same failures. | Optional preflight only. |
| CDP-EDGE | A clerk decides whether a printed copy-detection pattern is genuine, counterfeit or needs inspection using RGB/NoIR. | False acceptance enables counterfeit goods; unnecessary inspection costs time. | Low-cost spectral/edge verification of printed CDPs. | Oleksiyuk et al. 2026 already use cross-camera dual-synthetic references on low-end devices; Atoki WACV 2026 uses printer-signature-conditioned diffusion. | **KILL** | New detector, RGB/NoIR fusion, printer classification or Pi deployment overlaps direct endpoints. | Retain only as an application example or physical-transfer appendix. |
| VIEW-VERIFY | A benchtop logger turns a pan-tilt camera to verify an ambiguous or occluded printed marker. | A false retain corrupts a local record; an unnecessary move costs latency/energy and may disturb the fixture. | Viewpoint action evaluated by exact marker truth and retain/reacquire/unknown cost. | Huang et al. NeurIPS 2024 active viewpoint selection; Dai et al. uncertainty-driven pan-tilt planning; NBV/occlusion-aware planning family. | **KILL** | New view planning or confidence-triggered rotation collides; fixed second view or always-two-view policy matches. | Optional apparatus demo only. |
| POL-GLARE-VOI | A local logger decides whether a printed marker behind a transparent cover should be retained or reacquired after one polarizer intervention. | Glare can cause a false retained code; extra capture costs latency/energy. | Finite value-of-information test for a clip-on linear polarizer under matched cost. | Tang CVPR24 film removal/QR; PolarFree CVPR25 reflection reconstruction; Khandaker 2024 glare perception; Laser Shield DAC24 polarizer rotation. | **HOLD (Amber)** | Fixed filter/two-shot/scalar quality/digital deglare ties or labels are not independent. | Requires angle/lux/exposure/energy logs and static exact-marker oracle. |
| POL-WET-STATE | A bench operator decides whether a declared tile/film surface is dry, wet or unknown from RGB/NoIR plus a polarizer ratio. | Mislabeling a wet surface corrupts a maintenance record; extra inspection costs time. | Finite held-out-lamp/material-state map, not generic moisture sensing. | Polarized water/glare and leaf/material moisture literature; Venkatesulu AO 2025; Fu PLOS ONE 2024. | **PIVOT (Amber)** | RGB/fixed-light features match or state labels confounded by angle/material lot. | Optional physical cell or appendix. |
| POL-STRESS-TRUST | A fixture decides whether transparent plastic stress makes a marker capture untrustworthy. | False retain corrupts a record; mechanical probing costs time. | Bounded stress-induced decode-failure sentinel, no stress estimate. | Efferz 2024 photoelastic QC; MultiPolar 2026 load-step benchmark. | **PIVOT/KILL** | RGB blur/edge/saturation predicts equally or load labels drift. | No safety or MPa claim. |
| POL-SEC-ANGLE | A verifier uses multiple polarizer angles to detect a polarized-light attack. | False acceptance creates a security error. | Polarizer-angle attack defense. | Laser Shield DAC24 and Li CVPR24 directly occupy attack/defense. | **KILL** | Adaptive attacker or existing rotation defense defeats claim. | Do not pursue. |
| XFER-ACT | A lab maintainer swaps RGB/NoIR capture paths and chooses retain, reacquire or unknown for a non-personal printed marker. | False retain corrupts a maintenance record; extra captures cost time/energy. | Finite source-calibrated action-rank preservation audit on blocked camera/lamp cells with exact-code oracle and matched joules; no population transfer guarantee. | CCMNet (ICCV25), Lens (2026), and Ma et al. (2026) occupy cross-camera transfer/adaptive camera-health components. | **PIVOT (Amber)** | Fixed two-shot, scalar brightness/blur, or camera-specific threshold matches the held-out frontier; two-world observations make action choice non-identifiable. | Run preregistered finite pilot only; promote only repeatable rank inversion or report bounded null. |
| LUX-VOI | A logger uses an ambient-light reading to decide whether a second NoIR+IR capture is worth its cost. | False retain creates a bad bench log; needless capture consumes energy/latency. | Conditional value-of-information of an exogenous lux channel beyond image quality under matched-spectrum lamps. | CIE S 017 photopic lux definition, Lens adaptive control, and Zhang active illumination occupy the components. | **KILL thesis / PIVOT instrumentation** | Equal-lux spectra reverse RGB/NoIR utility, or luma/AE plus fixed two-shot dominates. | Do not buy a lux sensor for a thesis claim; matched-spectrum pilot may document insufficiency. |
| PANEL-GATE | A technician uses a small reference panel to decide whether a replacement camera needs reacquisition. | False release corrupts records; repeated panel captures delay work. | Panel sufficiency for exact-marker risk on held-out material/device cells. | Punnappurath 2025 warns one chart under-represents materials; chart metrology and Ma 2026 health monitoring are direct neighbors. | **KILL thesis / preflight** | Diffuse panel cannot witness local glare/focus; gray/white patch or fixed per-device threshold matches. | Optional camera-path preflight only. |
| CAL-SCHED | A maintainer schedules reference checks during a long bench run. | Skipped drift causes false records; checking too often wastes time. | Sequential trigger versus periodic recalibration at matched false-retain and panel cost. | Moravec & Sara 2024 and Wei et al. 2024 occupy online calibration monitoring/self-check scheduling. | **KILL thesis** | Periodic/every-frame/quality threshold schedule matches. | Keep only as maintenance characterization. |
| REAL-DIG-RANK | A benchmark maintainer tests whether digital corruption rankings predict physical RGB/NoIR fault rankings. | Reversed ranking can retain a bad capture or waste retakes. | Finite paired physical/digital action-rank inversion map with exact marker oracle. | Agnihotri CVPRW25, Liao, MultiCorrupt and MSC-Bench occupy broad synthetic-vs-real proxy testing. | **MERGED INTO PRF-TR** | Rankings correlate, simple transforms suffice, or physical labels are not independent. | Treat as the measurement protocol inside PRF-TR, not a second candidate. |
| IR-MOD-ID | A lab technician records whether a printed maintenance indicator is readable behind a named transparent cover or in clean low visible light. | False retain corrupts a maintenance record; unnecessary inspection consumes a scarce capture/operator slot. | Finite two-frequency PWM-IR intervention test: does frequency-response information change the action-optimal ranking beyond RGB-only, static NoIR+IR, fixed two-shot and scalar IR-ratio controls at matched joules? | Ma 2026 passive camera-health, Medici 2025 contamination, Han 2025 active FDI and Ke et al. 2026 transparent-material multi-light inspection occupy passive health, causal diagnosis and defect-detection mechanisms. | **HOLD (Amber)** | Fixed two-shot or scalar ratio is Pareto-optimal; response is not repeatable; or two independently labelled worlds remain observationally overlapping. | Add only after the minimal IR-CAUSE pilot; no causal-diagnosis, contamination-severity or generic adaptive-sensing claim. |
| SLOW-CAP | A lab maintainer records a printed indicator while Pi capture/inference runs under CPU, memory or I/O stress. | Finite stress-conditioned `retain / retry / unknown` policy using camera latency plus local telemetry, evaluated on held-out stress schedules. | Pourreza & Narasimhan UCC 2025, Bian 2026 and Stubbs 2025 occupy timeout, screen-confirm and Pi frame-queue mechanisms. | **KILL thesis / PIVOT replication** | Static timeout or latency-only EWMA has the same frontier, or telemetry does not change exact-code failure risk. | Bounded Pi/workload characterization only; no new timeout or adaptive-acquisition claim. |
| RAW-JPEG-GATE | A curator decides whether to retain or reacquire a printed code after one exposure. | Finite same-exposure RAW-domain saturation/SNR/channel-occupancy witness improves exact-code retain/reacquire risk at matched byte/latency cost over JPEG luma/blur/decoder confidence. | Chen et al. AAAI 2024 RAW detector/RCB and Berdan et al. ReRAW CVPR 2025 already occupy broad RAW robustness and edge-detection claims; NTIRE 2025 occupies RAW reconstruction. | **HOLD (Amber)** | JPEG-only, decoder confidence or fixed reacquisition matches; raw-mode overhead erases the benefit; or Camera Module 3 raw capture is unavailable. | Verify raw Bayer/DNG mode first, then preregister matched-budget single-exposure cells; no general RAW robustness claim. |
| NOIR-SIDEINFO | A local logger must send a readable printed maintenance indicator over a constrained local link. | Finite test of whether a sequential NoIR frame used as decoder-side information lowers total transmitted bytes at fixed exact-code false-retain, latency and energy versus independent JPEG/WebP, ROI JPEG and code-only transmission. | Yin ICRA 2024/D-CAN 2025 occupy decoder-side multi-view compression; TAGIC INFOCOM 2024 occupies task-guided communication. | **HOLD (Amber)** | NoIR must also be transmitted, registration error dominates, or code-only/ROI JPEG/fixed two-shot is Pareto-optimal. | Static paired-camera pilot with shaped Wi-Fi and independent payload oracle; no generic distributed-compression claim. |
| PROG-RET | A maintainer wants an early answer before a complete image arrives over a congested local link. | Deterministic base-layer/enhancement stopping chooses retain, request enhancement or unknown at fixed false-retain ceiling and total cost. | npj Wireless Technology 2025 and Kim et al. 2025 occupy progressive machine-oriented transmission/adaptive decoding. | **PIVOT (Amber)** | Fixed-high JPEG, code-only or progressive JPEG baseline matches; low-quality layer yields plausible wrong code; confidence is oracle-leaked. | Keep as a stopping/control experiment only, not a new codec. |
| TASK-CODEC-NULL | A curator is told a learned task-aware codec is needed for a small printed indicator. | Bounded negative test of whether tuned JPEG/WebP plus ROI already matches or beats a reproducible learned codec after Pi encode/decode cost. | TFIC EUSIPCO 2025, DMTUIC ECCV 2024, Guo ICLR 2025 and Ge CVPR 2024 occupy task-aware/OCR compression. | **HOLD (negative protocol only)** | Published codec/checkpoint unavailable, or learned codec wins after cost-inclusive held-out evaluation. | Optional negative appendix; do not generalize beyond declared marker/light/cover cells. |
| EXIF-WIT | A bench operator audits whether image metadata matches the actual exposure/control state. | Stale metadata can invalidate later quality or calibration conclusions. | Finite image-linear-statistics versus logged exposure consistency check detects injected metadata/control mismatches that predict marker failure. | Rajan et al. ISM 2025 is a direct exposure-consistency neighbor; full limitations remain unverified. | **PIVOT (Amber)** | Metadata is not consumed by the capture path, consistency adds no decision value, or the full direct paper closes the endpoint. | Preflight only until full ISM text and raw metadata support are verified. |
| FRAME-ADMIT | A logger receives redundant/blank frames while waiting for a printed event. | Queue pressure can drop the one valid event; aggressive filtering can miss it. | Deterministic temporal-difference/cooldown admission preserves exact event recall at fixed storage/inference cost. | Stubbs 2025 and mature event-triggered/adaptive-sampling work occupy the operational mechanism. | **KILL thesis** | Fixed period or motion threshold matches recall/cost, or queue pressure is absent. | Engineering baseline only. |
| PRIV-STORE | A non-personal lab logger chooses raw, redacted-code or discard storage. | Raw retention can leak a synthetic private attribute; discard can lose the task record. | Finite privacy-utility action frontier for stock-camera two-stage storage under Pi latency/storage limits. | PrivateEye WACV 2025 and Sogabe WACV 2025 occupy pre-capture/on-device privacy mechanisms. | **KILL thesis** | Blur/crop/delete-after-inference matches, or transient RAM prevents the claimed bound. | Synthetic privacy audit only. |
| CHAIN-REPLAY | A maintainer checks whether an offline RGB/NoIR capture sequence is complete and ordered. | Deleted, duplicated or reordered frames corrupt a reliability log. | Local append-only hash chain detects sequence tampering with measured overhead, without claiming scene truth. | ZIRCON JISA 2024, C2PA/NIST AI 100-4 and WWW Companion 2026 occupy provenance/integrity. | **KILL thesis** | Per-file hashes provide the same detection, or the attacker can rewrite the chain store. | Cryptographic control/negative demonstration only. |
| START-WIT | During mobile repair, a technician commands a non-personal low-voltage fan/motor to start and must record whether its shaft crosses a declared pulse/RPM rule before the deadline. | A false record can send the next maintenance step in the wrong direction. It never establishes pump/valve/process success. | Former current-plus-acceleration prefix policy with `running / failed-to-start / inspect`. | Motor start/fault supervision, direct tachometer/speed-switch run confirmation, edge motor sensing, early time-series classification and reject/cost decision rules are occupied. | **KILL as positive FYP mechanism** | The direct tachometer deadline policy observes the predicate more directly; withholding it makes the proxy policy an artificial restriction, while ETSC already occupies the prefix/reject mechanism. | Retain only as a bounded direct-witness-versus-proxy control; do not purchase a motor fixture for thesis validation. |
| CAUSE-NULL | A disconnected technician sees a changed non-personal temperature/current/vibration trace and must mark `equipment changed / sensor suspect / unknown`. | A wrong cause label sends repair to the wrong component; inspection is limited. | Former paired-world and minimum-witness proposal. | Formalized form is fault detectability/isolability and sensor placement; unformalized form is a trivial one-channel confounding example. | **KILL as primary FYP direction** | A staged pair plus reference channel is finite FDI replication, not a new theorem or mechanism. | Retain only as a negative control for a future physical candidate. |
| OFFLINE-REV-NULL | An owner of a non-critical lab fixture has cached authorization for a benign simulated job while disconnected. | A false allow after hidden revocation runs an unapproved bench job; false defer delays a benign one. | Former valid/revoked paired-world and freshness-witness proposal. | Cached-revocation freshness, OCSP/CRL status, leases, trusted time and revocation distribution are established controls. | **KILL as primary FYP direction** | Without freshness evidence the worlds are trivially indistinguishable; with it, normal expiry/update/introspection is the baseline. | Retain only as a protocol counterexample/control. |
| PIN-WIT | A technician would commission a replaced non-personal sensor harness using two MCU GPIO endpoints. | A wrong map could invalidate later bench records. | Former adaptive GPIO codebook proposal for `commission / inspect / unknown`. | Interconnect testing, boundary-scan architecture, BIST, functional loopback and adaptive diagnostic-test-pattern generation are established families. | **KILL as primary FYP direction** | A complete fixed fault-signature codebook has the same separability under the declared finite model; an adaptive tree only changes expected test order/cost. No real recurring harness-commission workflow is confirmed. | Retain only as a fixture-specific engineering/teaching control. |
| WATERMARK-CLOSE | A disconnected local logger would tell a technician whether a non-personal event-time window is `complete`, `late-but-usable` or `unknown`. | A false complete report hides a delayed/missing event; excessive unknowns create manual rechecks. | Local closure from sequence numbers, source epoch and a watermark. | Watermark semantics, Kafka grace/sequence components and asynchronous indistinguishability are direct boundaries. Without a conformant source closure witness, delayed-exists and never-existed worlds are identical locally; with it, fixed grace plus contiguous sequence is the baseline. | **KILL** | The claim is either unidentifiable or standard watermark/grace processing. | Retain only as a negative boundary example; do not implement as a thesis method. |
| SCHEMA-QUARANTINE | A mobile repair node would receive sensor records whose firmware, units, calibration epoch or aggregation meaning may have changed while a schema registry is unavailable. | A semantic mis-merge can create a false trend and repeat visit; excessive quarantine delays review. | Local `accept / convert / quarantine` inference from schema ID, metadata, provenance, range constraints and a finite trace. | Avro/SSN/SOSA/SensorThings/PROV/VIM already provide the declared syntax, semantic and provenance fields. A hidden RMS/peak or calibration-meaning change can preserve every permitted observation. | **KILL** | Explicit signed contract comparison is an established conservative baseline; hidden semantic preservation is unidentifiable without an independent witness. | Retain only as a bounded negative benchmark/example; do not implement as a positive FYP method. |
